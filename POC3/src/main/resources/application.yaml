spring:
  application:
    name: POC3
  datasource:
    url: jdbc:mysql://localhost:3306/SpringSecurity?useSSL=false
    username: root
    password: Pranay@666
  jpa:
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          format_sql: true
    show-sql: true